// Tailwind v4 reads theme tokens from CSS via @theme in src/styles/globals.css.
// This file exists for tooling that still looks for it; v4 doesn't require it.
export default {
  content: ["./src/**/*.{astro,html,js,jsx,ts,tsx,md,mdx}"],
};
